# Build context is the repo root, but the Dockerfile only COPYs install_qt6.sh
# and conanfile.txt. Exclude everything else so the context tarball stays tiny
# (no ~1GB .qt, no build/, no .git, no submodules).
*
!install_qt6.sh
!conanfile.txt
!versions.env
